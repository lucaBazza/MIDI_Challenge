<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:id="@+id/activity_login_activity"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:src="@drawable/backgrounds_login"
    android:background="@drawable/backgrounds_login"
    android:scaleType = "centerCrop"
    tools:context="midiapp.midi_challenge.Login_Activity">

    <GridView
        android:id="@+id/layout_griglia_utenti"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="0.8"
        android:background="@color/background_material_light_1"
        android:horizontalSpacing="20dp"
        android:numColumns="auto_fit"
        android:verticalSpacing="20dp"></GridView>


    <LinearLayout
        android:visibility="invisible"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/layout_inserimento_dati"
        android:layout_gravity="center_vertical"
        android:layout_weight="0.2">

        <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/edtTxt_nomeUtente"
            android:hint="Nome Utente..."
            android:maxLength="30"
            android:maxLines="1"/>

        <Button
            android:layout_height="wrap_content"
            android:text="Inserisci \n Utente"
            android:layout_marginLeft="50dp"
            android:id="@+id/btn_nuovo_utente"
            android:textColor="@android:color/white"
            android:background="@drawable/roundcorner"
            android:layout_width="match_parent" />
    </LinearLayout>

</LinearLayout>

