<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="midiapp.midi_challenge">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.categoy.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PaginaPrincipale_Activity" />
        <activity android:name=".Dettagli_Brano_Activity" />
        <activity android:name=".Aggiunta_Brano_Activity" />
        <!-- <activity android:name=".Registratore_Activity" /> -->
        <activity android:name=".AccordatoreActivity">
            android:name="AccordatoreActivity"
            android:label="@string/app_name">
        </activity>

        <activity android:name=".Login_Activity" />

    </application>

</manifest>
